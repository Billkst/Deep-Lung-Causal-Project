# Task19.5 收官报告（可答辩版）

## 1. 执行结论（Executive Summary）
- 结论：在既定协议（strict ablation + joint selection + 当前数据构造）下，Task19.5 已形成稳定边界，继续同口径迭代的边际收益极低。
- 最佳阶段性结果：`current_best_c15`（历史最优）为 `global=4/6`。
- 近阶段机制改造结果（V2~V6）：均未触发命中条件（`any_hit=false` 或未过 cls gate）。
- 最接近突破点：V6 将 `AUC gap vs w/o HGNN` 推进到 `-0.001907`，但 `Acc gap` 仍为 `-0.038835`，联合约束仍失败。

## 2. 审计范围与口径
- 数据口径：沿用项目现有半合成数据与分割流程。
- 评估口径：统一 6 指标（AUC/Acc/F1/PEHE/Delta CATE/Sensitivity）、pairwise/global 计数。
- 运行口径：全部采用 `screen` 后台、固定 conda 环境、日志与结果全量落盘。

## 3. 关键证据链（V2~V6）

| 轮次 | 目标策略 | 最优候选 | global_wins | AUC gap vs HGNN | Acc gap vs HGNN | 判定 |
|---|---|---|---:|---:|---:|---|
| V2 | 分类优先 + strict cls-gate | v2a_cls_bal | 3/6 | -0.017544 | +0.009709 | 未过门槛 |
| V3 | AUC过零优先（快速轮） | v3b_auc_push_fhp | 2/6 | -0.013730 | -0.067961 | 未命中 |
| V4 | 最小机制改造（约束+pred boost） | v4a_mech_base | 2/6 | -0.009153 | +0.000000 | 未命中 |
| V5 | 双头解耦最小实现 | v5c_nodetach | 2/6 | -0.004195 | -0.029126 | 未命中 |
| V6 | 自适应alpha（含Acc floor） | v6a_acc_guard | 2/6 | -0.001907 | -0.038835 | 未命中 |

**观察**：AUC 负差被持续压缩，但 Acc 同步恶化或维持负差，导致联合目标始终不达标。

## 4. 根因归纳（答辩可复述）
1. **多目标冲突显著**：分类目标与因果目标在当前表示/损失耦合下存在稳定冲突。
2. **边界特征清晰**：AUC 可被推到接近 0，但 Acc 负差回落，体现为“边界附近震荡”。
3. **协议内迭代趋于饱和**：从纯调参到轻机制改造（V4~V6）均未跨边界，说明不是“搜得不够”，而是“机制上限受限”。

## 5. 已完成的工程闭环
- 代码侧：V4/V5/V6 机制均已落地并可复跑。
- 运行侧：全部使用 `screen` 执行，JSONL 过程日志可审计。
- 文档侧：每轮都已记录于 `docs/工作过程.md` 与 `docs/项目结构说明.md`。

## 6. 对外答辩建议话术（简版）
- 我们已从“参数搜索”升级到“机制改造”，连续 V4/V5/V6 仍未跨过联合边界，证明问题不是调参深度不足。
- 现阶段最合理决策是停止同口径盲跑，将贡献转为“边界识别与机制解释”，并进入下一阶段协议重定义或结构重构。

## 7. 下一阶段建议（仅给可执行选项）
- 方案 A（推荐）：重定义成功标准为“分类不劣 + 因果稳健”的统计显著门槛，结束 Task19.5。
- 方案 B：进入新课题（Task19.5-Next），允许结构级重构（真正双分支 backbone + 冲突梯度求解），重新立项。

## 8. 最终结论
- Task19.5 在当前协议下应 **收官**：结论充分、证据完整、可复现可答辩。
- 不建议继续同口径消耗算力做盲目冲刺。
